import React from 'react';

const BuildingPage = () => {
  return (
    <div>
      <h2>Building Page Components</h2>
      <p>
        The core of any React application is its components. Components are reusable, self-contained units that manage their state and can be composed to build complex UIs.
      </p>
      <p>
        In this section, we'll dive into creating components, managing state, and understanding the component lifecycle.
      </p>
    </div>
  );
};

export default BuildingPage;
